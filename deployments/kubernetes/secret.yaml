# Placeholder secret — `make k8s-secret` regenerates this from .env
# (kubectl create secret generic app-secret --from-env-file=.env).
# Never commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: go-enterprise
type: Opaque
stringData:
  APP_GATEWAY_TOKEN: "change-me-gateway-token"
  APP_JWT_SECRET: "change-me-jwt-secret"
  POSTGRES_PASSWORD: "app"
  OAUTH_CLIENT_SECRET: "mock-secret"